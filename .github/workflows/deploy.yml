name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # repo1 (React + Vite)
      - name: Checkout repo1
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # repo2 (Astro with Bun) -> ./sites/sr-lang-docs
      - name: Checkout repo2 (Astro)
        uses: actions/checkout@v4
        with:
          repository: theunnecessarythings/sr-lang-docs
          ref: main
          path: sites/sr-lang-docs
          # token: ${{ secrets.REPO2_TOKEN }}  # only if repo2 is private

      # Node for repo1 (npm)
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      # Build repo1 (npm)
      - name: Install dependencies (repo1)
        run: npm ci
      - name: Build (repo1)
        run: npm run build   # -> ./dist

      # Bun for repo2
      - name: Set up Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      # Build repo2 (Bun)
      - name: Install dependencies (repo2)
        working-directory: sites/sr-lang-docs
        run: bun install
      - name: Build (repo2)
        working-directory: sites/sr-lang-docs
        run: bun run build   # -> ./sites/sr-lang-docs/dist

      # Merge Astro build at /language
      - name: Merge outputs
        run: |
          mkdir -p dist/language
          rsync -a sites/sr-lang-docs/dist/ dist/language/
          touch dist/.nojekyll
          # React SPA fallback (keep this after confirming /language works)
          cp dist/index.html dist/404.html || true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
